.DS_Store
node_modules
/build
static
/.svelte-kit
/package
.env
.env.*
!.env.example
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions
.editorconfig
.vscode
src/routes/test
vite.config.ts.timestamp-*
.claude
DOCKER_BUILD.md
MONITOR.md
dev.yml
images.yml

# Playwright / playwright-bdd
.features-gen
test-results
playwright-report
blob-report
.playwright

# Vitest browser mode: screenshots and attachments written on failure only.
.vitest-attachments
__screenshots__

# Local test-runner overrides; the .example template stays tracked
# (needed because the broad `.env.*` rule above would hide it).
.env.test-all
!.env.test-all.example
